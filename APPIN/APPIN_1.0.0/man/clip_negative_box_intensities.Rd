% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_peak_picking.R
\name{clip_negative_box_intensities}
\alias{clip_negative_box_intensities}
\title{For each bounding box, sum the raw spectrum intensity inside the box. If
the sum is < 0, set the matching peak's intensity column to 0 in \code{peaks_df}.
Boxes and peaks are kept (coordinates preserved); only the intensity is
forced to 0. This is consistent with the post-integration behaviour in
\code{mod_integration.R} and the export behaviour in \code{mod_export.R}.}
\usage{
clip_negative_box_intensities(peaks_df, boxes_df, spectrum_matrix)
}
\arguments{
\item{peaks_df}{Data frame with \code{stain_id} and an intensity column
(\code{stain_intensity} for CNN output, \code{Volume} for local-max output).}

\item{boxes_df}{Data frame with \code{stain_id}, \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}.}

\item{spectrum_matrix}{Raw 2D NMR matrix (rownames = F1 ppm, colnames = F2 ppm).}
}
\value{
List with \code{peaks_df} (clipped) and \code{n_clipped} (count).
}
\description{
For each bounding box, sum the raw spectrum intensity inside the box. If
the sum is < 0, set the matching peak's intensity column to 0 in \code{peaks_df}.
Boxes and peaks are kept (coordinates preserved); only the intensity is
forced to 0. This is consistent with the post-integration behaviour in
\code{mod_integration.R} and the export behaviour in \code{mod_export.R}.
}
